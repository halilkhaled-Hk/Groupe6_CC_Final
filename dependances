pip install pre-commit flake8 black
pip freeze > requirements.txt

deplus dans le projet tapez :
python -m venv venv
source venv/bin/activate  # (sous Windows: venv\Scripts\activate)

pour ouvrir :
uvicorn main:app --reload
